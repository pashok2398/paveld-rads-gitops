apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-server
  namespace: argocd
spec:
  entryPoints:
    - web  # HTTP entry point (port 80)
  routes:
  - match: Host(`argocd.example.com`)
    kind: Rule
    services:
    - name: argocd-server
      port: 80

